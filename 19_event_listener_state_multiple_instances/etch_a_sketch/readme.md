Requirement:
Make an Etch a sketch

- Create a turtle, that can do:
  - move forward when `w` is pressed,
  - move backward when `s` is pressed,
  - facing toward counter-clockwise when `a` is pressed,
  - facing toward clockwise when `d` is preseed.
  - clear drawing when `c` is pressed. (note: turtle pos reset back to center/starting position when `c` to pressed)
